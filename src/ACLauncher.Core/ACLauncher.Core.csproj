<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>ACLauncher.Core</RootNamespace>
    <AssemblyName>ACLauncher.Core</AssemblyName>
    <!-- Library code never resumes on a captured context, and every disposable is disposed. -->
    <AnalysisModeReliability>All</AnalysisModeReliability>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="ACLauncher.Tests" />
  </ItemGroup>

</Project>
